actions:
  hakyll-main:
    - on: call
      run_pipelines:
        - hakyll-main

    - on: changed
      run_pipelines:
        - hakyll-main
      repo_id: main
      changes:
        - '^public/.*'

    - on: changed
      run_pipelines:
        - hakyll-main
      repo_id: main
      changes:
        - '^builder/.*'

  publish-main:
    - on: call
      run_pipelines:
        - publish-main
      params:
        build-org-publish-image: true
        clean-caches: false

  publish-main-rebuild:
    - on: call
      run_pipelines:
        - publish-main
      params:
        build-org-publish-image: true
        clean-caches: true
